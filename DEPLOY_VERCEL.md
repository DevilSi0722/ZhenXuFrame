# Vercel 简化部署：浏览器 IndexedDB

不需要数据库、图片存储或远程服务器。在 Vercel 的 **Optional Integrations** 页面可直接跳过 Upstash Redis。

## 部署步骤

1. 将源代码提交到 GitHub，然后在 Vercel 导入仓库。不要提交 `.local`、真实 `.env`、`node_modules` 或 `dist`；这些已列入 `.gitignore`。
2. 框架选择 **Vite**，构建命令 `npm run build`，输出目录 `dist`，Node.js 22.x 或 24.x。
3. 添加以下两个必需环境变量，点击 Deploy；已部署的项目修改变量后需 **Redeploy**。

| 变量 | 内容 |
| --- | --- |
| `FRAME_PASSWORD` | 自己的工作台登录密码，至少 16 位，推荐随机密码 |
| `DEEPKEY_API_KEY` | 具有 Seedance 权限的 DeepKey Key |

可选变量 `DEEPKEY_BASE_URL` 默认为 `https://deepkey.top`。可选 `FRAME_SECRET` 用于独立会话签名，设置时至少 32 位；不设置也能使用。不要给密钥变量加 `VITE_` 前缀。

4. 打开网站，输入工作台密码。在「连接设置」点击「测试连接」，随后即可生成视频。

`vercel.json` 已配置 API 路由和 300 秒函数上限。请启用 Fluid Compute，并确保项目允许这一执行时间。函数仅等待平台受理（最多 180 秒），不会等待整个视频生成。Vercel 函数、带宽和 DeepKey 生成仍受各自套餐及计费规则约束。

## 日常使用

- **任务和原始参考图存储在当前浏览器的 IndexedDB**。刷新页面、普通关闭后重新打开可继续查看；清除网站数据、无痕窗口结束或浏览器回收存储可能使记录丢失。
- 同一设备的其他浏览器、其他设备、不同域名使用独立记录。建议部署后固定使用一个域名；切换域名前先导出备份。
- 在「任务历史」中点击 **导出备份 / 恢复备份** 可迁移记录和图片。恢复只增加新记录，不覆盖已有记录，不发送生成请求。单个导入文件上限 200 MB，最多 10000 条记录。
- 备份包含提示词、参考图片和视频链接，不包含 API Key、工作台密码或视频文件。完成的视频仍应单独下载保存，平台链接可能过期。
- 已提交的图片可从任务复用。尚未提交的图片只在当前页面内存中，刷新后需重新选择；文本草稿保存在 LocalStorage。
- 参考图最多 9 张，单张输入最多 10 MB。提交时浏览器按总预算压缩为 JPEG，必要时缩小尺寸，JPEG 不支持的透明区域变为白底；较小图片保留原格式。原始图片随任务保存在 IndexedDB。整个 API 请求限制为 3.8 MB，留出 Vercel 请求上限余量；需要保留原始品质可使用图片公网直链。

## 提交与恢复

任务先在浏览器事务中保存，再发送生成请求。浏览器无法保存时会阻止提交；相同请求标识在同一网站的多个标签页间通过 IndexedDB 原子去重。这不是跨设备或服务端的全局幂等保障。

**正在提交时请保持页面打开，等平台返回任务 ID 后再关闭。** 关闭页面后，平台已受理的视频仍继续生成；再次打开网页会恢复查询。页面可见时约每 15 秒查询一批最多五个任务，重试只针对状态查询。

提交断线、超时、响应缺少 ID 会记录为「提交待确认」，不自动重发。刷新后超过四分钟仍未得到 ID 的提交也显示待确认。此时应先查 DeepKey 记录，再通过「导入任务」输入 ID；不要直接重新生成。

浏览器在平台受理后突然被关闭或写入失败，可能无法保存平台 ID，这是无服务端持久存储的限制。如果网页提示已收到 ID 但保存失败，请立即复制 ID 或导出备份。

## 密钥与访问

API Key 只存在 Vercel 环境变量，网页不能读取或保存真实密钥。更改地址/Key 后，属于旧连接的任务暂停查询；恢复原配置后可继续。更改工作台密码会使原有会话失效。

登录会话通过 Secure、HttpOnly、SameSite Cookie 保存，有效期七天。登录有函数实例内的尝试次数限制，不提供跨实例全局限流；如需更强保护可使用 Vercel Firewall。此应用是个人工作台，不提供多用户数据隔离；退出登录不会删除当前设备的浏览器记录。

## 从旧版迁移

- **旧本地版**：原有 `.local` 文件保留，不会被自动读取或上传。可在新网页「恢复备份」选择本机 `.local/jobs.json`，恢复其中的参数和参考图（仅在自己的工作台使用）。旧记录没有新版连接标识，需按平台任务 ID 重新导入以恢复查询；导入不会重新生成。
- **旧 Redis/Blob 版**：新版本不会再访问这两个服务，已有云端记录不会自动迁移。已有任务可按 ID 导入，旧版私有 Blob 图片需要重新添加。确认需要的旧数据已留存后，可自行断开集成。不要直接删除需要保留的数据。
- 更新后的项目不再使用 `UPSTASH_REDIS_REST_*`、`KV_REST_API_*`、`BLOB_READ_WRITE_TOKEN` 和 `FRAME_NAMESPACE`；旧环境变量留着也不会被读取。

## 本地开发与验证

`npm run dev` 或 `npm run build` 后 `npm start` 仍使用原来的本机文件存储与本机 Key，不影响已有本地数据。Vercel 部署入口为 `api/handler.mjs`，使用浏览器存储模式。

`npm test` 覆盖模拟 IndexedDB 事务、备份恢复、存储失败阻止提交、断线不重复提交、API 鉴权与请求大小限制。`npm run build` 构建前端。真实 Vercel 和 DeepKey 联调需要部署后完成；自动测试不读取个人凭据、不生成收费视频。
