# Vercel workspace login only. Never commit real secrets.
FRAME_PASSWORD=replace-with-a-random-password-at-least-8-characters
# Optional session signing secret, at least 32 characters.
# FRAME_SECRET=
# Each visitor configures their own DeepKey Key in the browser.
# Cloud deployment does not read DEEPKEY_API_KEY or DEEPKEY_BASE_URL.
